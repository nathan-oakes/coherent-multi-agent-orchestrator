# coherent-multi-agent-orchestrator

A continuity-first, anti-sycophancy multi-agent dialogue system.

## Quick start
```bash
python -m venv .venv && source .venv/bin/activate
pip install -e .
cmao
```
